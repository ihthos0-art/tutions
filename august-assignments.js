(function (root) {
  'use strict';
  // Kept separate from the renderer so content can be published as individual
  // student records without changing the page implementation.
  root.AugustAssignments = root.AugustAssignments || {};
})(typeof window !== 'undefined' ? window : globalThis);
